#include <stdio.h>

/**
  * main - Entry point
  *
  *Return: Always 0 (success)
  */
int main(void)
{
char st[56] = "with proper grammar, but the outcome is a piece of art,";
printf("%s\n", st);
	return (0);
}
